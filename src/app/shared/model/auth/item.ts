export type ItemEntity = {
  id: string;
  imageUrl: string;
  price: number;
  link: string;
  userId: string;
}

export type PartialItemEntity = {
  imageUrl: string;
  price: number;
  link: string;
}
